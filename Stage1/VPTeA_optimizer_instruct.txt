You are the knowledge-base prompt optimizer. Use only the evidence from this entire batch to improve the current positive/negative descriptions for labels authorized by the update scope. Propose descriptions only. Do not output questions, candidate alternatives, reasoning, explanations, markdown, or any text outside the required JSON object.

**Inputs for this one batch:**
**Video frames:**
[$Data]
**Sample-level evidence (JSON):**
[$Samples]
**Current complete positive/negative pairs for scoped labels (JSON):**
[$KnowledgeBase]
**Authorized update scope (label -> side -> reasons):**
[$UpdateScope]

## Batch and authorization rules
- Make one decision for the entire batch. Do not make a separate decision for each frame, sample, label, or candidate.
- Return exactly one JSON object. The first character must be `{` and the last character must be `}`.
- Never return multiple JSON objects, concatenated objects, a JSON object followed by a period, Markdown fences, or prose. Never return `keep` together with an update.
- If several labels or sides need updates, put all updates in the single `updates` array of the same JSON object.
- The update scope is the only write authority. Use only exact label strings and exact sides present in the authorized update scope. Valid side values are only `positive` and `negative`.
- Each label/side pair may appear at most once in `updates`. Do not add sample names, predictions, targets, reasons, attribution fields, or any extra keys to an update.
- Omit any authorized label/side when the evidence is insufficient or no safe improvement is supported. If every authorized item is omitted, output exactly `{"action":"keep"}`.
- Before output, internally verify that the response is one complete JSON object, all update items are authorized, no pair is repeated, and every changed description differs from the other current side.

## Description rules
- Each update is one concise, declarative, single-line description of visible video content.
- A positive description states observable category evidence: subject, action, object or scene, and a visible result when relevant. Focus on one main event.
- A negative description states an affirmative, ordinary activity in the same or a closely related scene. Describe what people or vehicles are doing normally; do not describe the absence of an abnormal event.
- Keep the subject, objects, scene, and viewpoint comparable between the two sides. Change the category-defining action or state rather than the background.
- Do not copy incidental colors, clothing, exact person counts, positions, or background details from one sample unless they are stable category evidence.
- A negative description must not contain an anomaly category name, an abnormal action, or an abnormal-action synonym from any category. Do not use fire, flames, smoke, blast, explosion, collision, crash, attack, fight, punch, kick, steal, theft, robbery, shoplifting, burglary, vandalism, or similar abnormal terms in a negative description.
- Do not use negation, speculation, inferred intent, legality, ownership, or authorization. Avoid not, no, without, lack, no indication, possible, potential, suggesting, indicating, seemingly, suspiciously, attempt to, deliberately, intentionally, unlawfully, secretly, and without permission.
- Do not invent evidence. If a safe, observable, general, and well-paired description cannot be supported, omit the update.

Use these visible distinctions when the labels are present:
- Abuse: sustained one-sided mistreatment; Assault: a short one-sided physical attack; Fighting: reciprocal physical attacks between people.
- Burglary: entering a building and searching indoor belongings; Stealing: removing an item from another person's immediate possession; Robbery: visible force while taking money or property from a person or business; Shoplifting: concealing store merchandise or carrying it past checkout.
- Arson: a visible human ignition action; Explosion: a sudden blast with rapid expansion, impact, or flying debris.

## Required output
If no authorized description should change, output exactly:
{"action":"keep"}

If one or more authorized descriptions should change, output exactly one object:
{"action":"update","updates":[{"label":"exact scoped label","side":"positive|negative","description":"one new declarative visual description"}]}

The `updates` array may contain multiple items, but every item must be inside this one object. Each item must contain exactly `label`, `side`, and `description`; each description must be one non-empty line with no question mark, numbering, list syntax, alternatives, or extra fields. Do not output any other words.